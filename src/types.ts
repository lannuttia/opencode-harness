/**
 * Type definitions for the OpenCode Harness plugin
 */

/**
 * Plugin configuration options (if any)
 */
export interface HarnessConfig {
  // Currently no configuration options, but this interface exists for future extensibility
}
